{
  "credentials": [
    {
      "id": "llm-config",
      "type": "custom",
      "name": "LLM Model",
      "description": "Select a model for summarization. Leave empty to auto-detect the cheapest available provider.",
      "fields": [
        {
          "key": "llm_model",
          "label": "Model",
          "type": "model-select",
          "required": false
        }
      ],
      "storeAs": "skill.summarize"
    }
  ]
}
