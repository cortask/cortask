{
  "credentials": [
    {
      "id": "api-key",
      "type": "api-key",
      "name": "OpenAI API Key",
      "description": "OpenAI API key for Whisper transcription",
      "instructions": "Get your API key from https://platform.openai.com/api-keys",
      "fields": [
        {
          "key": "apiKey",
          "label": "OpenAI API Key",
          "type": "secret",
          "required": true,
          "placeholder": "sk-proj-••••••••••••••••••••••••••••••••",
          "validation": {
            "pattern": "^sk-(proj-)?[a-zA-Z0-9]{20,}$",
            "message": "Must be a valid OpenAI API key (starts with 'sk-' or 'sk-proj-')"
          }
        }
      ],
      "storeAs": "provider.openai.apiKey"
    }
  ]
}
